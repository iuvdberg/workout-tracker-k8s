# stringData values are stored as base64 by Kubernetes automatically.
# Change these values before deploying to a real environment!
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: workout-tracker
type: Opaque
stringData:
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres
  POSTGRES_DB: workout_tracker
